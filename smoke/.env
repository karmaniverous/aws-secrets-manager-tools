# Smoke test defaults (safe to commit).
#
# Optional overrides:
#   - Copy smoke/.env.local.template -> smoke/.env.local
#   - Edit values as needed

# AWS profile used by smoke tests (SSO profile is expected).
SMOKE_AWS_PROFILE=JGS-SSO

# Set to 1 to keep generated artifacts (e.g. fixtures/.env.local) after runs.
SMOKE_KEEP_ARTIFACTS=0
