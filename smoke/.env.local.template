# Smoke test local overrides (DO NOT COMMIT)
#
# Steps:
# 1) Copy this file to smoke/.env.local
# 2) Edit values below
#
# This repo's smoke scripts load:
#   - smoke/.env (committed defaults)
#   - smoke/.env.local (optional overrides; gitignored)
#
# These variables are passed into the spawned get-dotenv CLI process so that:
#   - CLI-flag smoke test can use --profile <SMOKE_AWS_PROFILE>
#   - Config-overlay smoke test can interpolate "$SMOKE_AWS_PROFILE" in config

# SMOKE_AWS_PROFILE=YOUR_PROFILE_HERE
# SMOKE_KEEP_ARTIFACTS=1
